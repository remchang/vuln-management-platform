# 扫描授权范围说明（scope.md）

> 本文件是《开源软件与新技术》实验07 的**授权记录**。
> 平台在执行任何扫描动作**之前**必须先校验这里的范围，校验不通过一律拒绝。

---

## 一、资产与授权

| 项 | 内容 |
| --- | --- |
| 被测系统 | `mubiao-demo`（本人自己写的本地演示应用，见 `target/app.py`） |
| 资产所有者 | 王锐兵（本人，软件2304，学号 23110506126） |
| 批准人 | 课程实验指导教师 |
| 授权依据 | 课程实验指导书《实验07 软件安全检测与漏洞管理平台》 |
| 目标 URL | `http://localhost:8000`、`http://127.0.0.1:8000` |
| 允许时间窗口 | 课程实验期间（本机离线时间，无固定时段限制） |
| 联系人 | 王锐兵 |

**关键一点**：被测系统是**我自己写的、跑在自己电脑上的**应用。
不是别人的系统，不是学校生产系统，也不是任何公网资产。

---

## 二、允许的动作

| 动作 | 是否允许 | 说明 |
| --- | --- | --- |
| 被动检查（pscan） | ✅ 允许 | 只发普通 HTTP 请求并检查响应 |
| 已知 URL 的主动请求 | ✅ 允许 | 仅限白名单内的明确地址 |
| 容器基线扫描（zap-baseline.py） | ⚠️ 有条件 | 需要 Docker；**本机没有 Docker，未执行** |
| 读取本地报告 | ✅ 允许 | |
| 修改本地目标源码并复扫 | ✅ 允许 | 这是本实验的修复闭环 |

## 三、明确禁止的动作

| 动作 | 状态 |
| --- | --- |
| 扫描任何公网域名 | ❌ **一律禁止** |
| 扫描他人系统（含学校任何生产系统） | ❌ 禁止 |
| 主动扫描（ascan / active scan） | ❌ 本次未批准 |
| 认证扫描、爆破、注入利用 | ❌ 禁止 |
| 对任何目标做拒绝服务式压测 | ❌ 禁止 |
| 使用真实账号、真实个人数据 | ❌ 禁止（目标应用只用虚构数据） |
| 把"私网地址"当成已授权 | ❌ 禁止（必须显式写进 `allowed_ips`） |

---

## 四、白名单配置

实际生效的配置在 `platform/fanwei-peizhi.json`，摘要：

| 类别 | 值 |
| --- | --- |
| `allowed_hosts` | `localhost`、`127.0.0.1`、`host.docker.internal`、`mubiao` |
| `allowed_ips` | `127.0.0.1` |
| `allowed_urls` | `http://localhost:8000`、`http://127.0.0.1:8000`、`http://host.docker.internal:8000` |
| `forbidden_keywords` | `example.com`、`evil.com`、`attacker` |

**校验规则**：

1. 协议必须是 `http` / `https`（其余一律拒）。
2. 主机名必须命中 `allowed_hosts`；或者是 IP 且命中 `allowed_ips`。
3. **私网地址不会自动放行**。`192.168.x.x` / `10.x.x.x` 必须显式写进
   `allowed_ips`，否则拒绝。
4. 完整 URL（含端口）如果出现在 `allowed_urls` 里，直接放行；
   否则回落到主机名规则。
5. 主机名里出现 `forbidden_keywords` → 直接拒。
6. **重定向到白名单外的主机要立刻停下**（`jiaoyan_zhongdingxiang`）。
   不能只校验初始 URL 就假定后续访问都获授权。

---

## 五、紧急停止方法

| 方式 | 操作 |
| --- | --- |
| 停掉被测目标 | `Ctrl+C`，或 `.scripts/qidong-mubiao.ps1` 起的那个进程窗口 |
| 停掉 ZAP | 关掉 ZAP 进程；容器路线用 `docker compose down` |
| 平台侧 | 平台的扫描命令生成函数**先过白名单校验**，校验不过直接抛异常，没有绕过入口 |
| 报告留痕 | 所有拒绝事件都记录拒绝原因与命中规则，便于事后核查 |

---

## 六、数据保护

| 项 | 做法 |
| --- | --- |
| 被测数据 | 全部是虚构数据，无真实用户信息 |
| 报告内容 | ZAP 报告可能含 URL、参数、技术栈信息；提交前做脱敏 |
| 报告存放 | 限制在本机项目目录，不对外发布 |
| 凭据 | 本实验未使用任何真实凭据；`.env` 与 `*.key` 已在 `.gitignore` |
| 演示结束 | 轮换临时 Token（本实验未产生）、清理测试账号（本实验未创建） |

---

## 七、★ 本机限制（必须如实记录）

**本机没有安装 Docker，也没有安装 OWASP ZAP。**

因此：

- **没有对任何目标执行过真实的 ZAP 扫描**，一次都没有。
- `reports/zap-before.json` 和 `reports/zap-after.json` 是
  **人工构造的测试夹具**（按 ZAP Traditional JSON 的真实结构编写），
  只用于验证"解析 → 指纹去重 → 状态流转 → 前后对比"这套逻辑。
- 夹具**不代表**对任何系统的真实扫描结果。

这个限制在 README、实验报告和测试记录里都写明了，
**没有把夹具结果表述成真实扫描结果**。
